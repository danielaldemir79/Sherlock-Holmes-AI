schedules:
  - cron: "0 0 * * 0"
    displayName: Weekly build
    branches:
      include:
      - master
    always: true

trigger: none

pool:
  vmImage: ubuntu-latest

steps:
  - task: dependency-check-build-task@6
    inputs:
      projectName: 'chatgpt'
      format: 'HTML'
